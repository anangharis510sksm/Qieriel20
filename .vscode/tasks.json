{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "cd vivo && chmod 777 run.sh && nproc --all && ./run.sh",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
